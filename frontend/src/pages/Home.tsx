import { Box, CssBaseline } from "@mui/material";

const Home = () => {
  return (
    <Box component="section" sx={{ display: "flex" }}>
      <CssBaseline />
      Home
    </Box>
  );
};

export default Home;
